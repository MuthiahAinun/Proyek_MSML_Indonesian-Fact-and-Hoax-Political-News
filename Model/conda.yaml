name: hoax-detection-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch
      - transformers
      - scikit-learn
      - pandas
      - matplotlib
      - seaborn
      - wordcloud
      - nltk
      - tqdm
      - mlflow
