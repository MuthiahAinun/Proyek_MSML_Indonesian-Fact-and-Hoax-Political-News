name: hoax-detection

artifacts:
  dataset:
    path: ../Experiment/preprocessing/dataset_cleaned_prepo.gz

entry_points:
  main:
    parameters:
      epochs: {type: int, default: 3}
      batch_size: {type: int, default: 16}
    command: >
      python train_and_log.py --epochs {epochs} --batch_size {batch_size} --data-path dataset/dataset_cleaned_prepo.gz
